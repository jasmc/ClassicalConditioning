"""Sensitivity and benchmark tools; excluded from the routine pipeline.

Review note: these modules are active comparison tools, not historical source,
but normal ``run-pipeline`` execution deliberately does not invoke them.
"""
